:root{
  --black: #0b0b0c;
  --gold: #d4af37;
  --white: #ffffff;
  --gray: #bbbbbb;
  --maxw: 1100px;
}

body{
  margin:0;
  font-family: "Georgia", serif;
  background: var(--black);
  color: var(--white);
  line-height: 1.7;
}

.container{
  max-width: var(--maxw);
  margin:auto;
  padding:20px;
}

/* Header */
.topbar{
  background: var(--black);
  border-bottom: 2px solid var(--gold);
  padding: 15px 0;
}

.header-flex{
  display:flex;
  justify-content:space-between;
  align-items:center;
}

.logo{
  font-size: 1.6rem;
  font-weight: bold;
  letter-spacing:1px;
  color: var(--gold);
}

.menu a{
  color: var(--gray);
  margin-left:20px;
  text-decoration:none;
}
.menu a:hover{
  color: var(--gold);
}

/* Hero */
.hero{
  position:relative;
  background:url('https://images.unsplash.com/photo-1528747045269-390fe33c19f2?auto=format&fit=crop&w=1600&q=80')
    center/cover no-repeat;
  height:350px;
  display:flex;
  align-items:center;
  text-align:center;
}

.overlay{
  position:absolute;
  top:0; left:0;
  width:100%; height:100%;
  background:rgba(0,0,0,.6);
}

.hero-content{
  position:relative;
  width:100%;
  color:var(--white);
}

.hero h1{
  font-size:2.3rem;
  color: var(--gold);
  margin-bottom:10px;
}

.btn{
  background: var(--gold);
  padding:10px 20px;
  color:black;
  border-radius:4px;
  text-decoration:none;
  font-weight:bold;
}

/* Cards */
.card{
  background:#111;
  border:1px solid #333;
  border-left:5px solid var(--gold);
  padding:25px;
  margin:20px 0;
  border-radius:4px;
}

.card h2{
  color:var(--gold);
}

.card ul{
  list-style:none;
  padding-left:0;
}

.card ul li{
  padding:5px 0;
  color:#ddd;
}

.card.highlight{
  background:#1a1a1a;
  border-left:5px solid var(--gold);
}

/* Footer */
.footer{
  text-align:center;
  padding:15px;
  border-top:1px solid #333;
  color:var(--gray);
}
